# Lobby server

- Runtime JAR name: `paper.jar`
- Runtime Config and world data are generated locally.
- Copy/merge the `*.example` files only after comparing them with the exact generated version.
- Do not commit JARs, worlds, logs, secrets or paid content.
